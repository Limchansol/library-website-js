class User {
    constructor(id, pw, name, gender, bd, cp) {
      this.id = id;
      this.pw = pw;
      this.name = name;
      this.gender = gender;
      this.bd = bd;
      this.cp = cp;
    }
    reserveLending(book, date) {
        return;
    }
  }

export default User;